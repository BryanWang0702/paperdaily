name: Daily literature refresh

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - 'src/**'
      - 'config.yaml'
      - 'requirements.txt'
      - '.github/workflows/daily.yml'
  schedule:
    # 07:15 and 20:15 Asia/Taipei / Beijing time (UTC+8).
    # Both runs avoid DeepSeek peak windows (09:00-12:00 and 14:00-18:00),
    # with extra margin before the morning peak in case GitHub Actions starts late.
    - cron: '15 23,12 * * *'

permissions:
  contents: write

concurrency:
  group: paperdaily-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - run: pip install -r requirements.txt
      - name: Fetch and rank literature
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python -m src.pipeline --days 3
      - name: Commit changed data
        run: |
          git config user.name "paperdaily-bot"
          git config user.email "paperdaily-bot@users.noreply.github.com"
          git add data site/data/latest.json site/data/archive.json
          if git diff --cached --quiet; then
            echo "No data changes"
          else
            git commit -m "Update daily literature"
            git push
          fi
